export function areAnagrams(first: string, second: string): boolean {
  // TODO: normalize both strings by lowercasing and removing spaces.
  // TODO: return false early when normalized lengths differ.
  // TODO: compare character frequencies without mutating the inputs.
  return false;
}

export const sampleFirst = "Dormitory";
export const sampleSecond = "Dirty room";

console.log(areAnagrams(sampleFirst, sampleSecond));
